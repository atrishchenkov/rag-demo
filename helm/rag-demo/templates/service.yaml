apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-demo.fullname" . }}
  labels:
    {{- include "rag-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rag-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
